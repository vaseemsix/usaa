	/* What is Array? 
		 * Array is an object of same data type that holds fixed number of values 
		 * length of an array is established when the array is created
		 * array has 0 based index and its length is fixed
		 * Declaration of Arrays:
		 * dataType  arrayName[];
		 * arrayName= new dataType[Size];
		 * arrayName[0] = value;
		 * arrayName[1] = value2;
		 * */

package pac;

public class Array {

	public static void main(String[] args) {
	int a[];
	String str[];
	a= new int[3];
	str=new String[5];
	System.out.println(str);
	
	}
}
